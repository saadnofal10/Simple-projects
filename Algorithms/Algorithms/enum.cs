namespace Algorithms
{
    public class @enum
    {

        public static void M(char c, Class2 ty)
        {
            Random random = new Random();
            random.Next(c, 'z');
            switch (ty)
            {
                case Class2.Asc:

                    break;
                case Class2.Desc:

                    break;
                case Class2.Random:

                    break;
            }
        }
    }
    public enum Class2
    {
        Asc,
        Desc,
        Random
    }
}
